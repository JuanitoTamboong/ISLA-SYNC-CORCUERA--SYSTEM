# TODO: Replace Image URL with Image Upload in Admin News

## Plan
Replace the text-based "Image URL" input in the admin news create/edit modal with a file upload interface (with preview), matching the pattern already used in the resident report page.

## Steps
- [x] 1. Update `pages/admin/admin-news.html` — replace text input with upload area + preview
- [x] 2. Update `js/admin/admin-news.js` — add file handling, preview logic, and wire to form submit
- [x] 3. Update `css/admin/admin-news.css` — add styles for upload box, preview, and remove button
- [x] 4. Test create news with image, without image, and edit existing news

