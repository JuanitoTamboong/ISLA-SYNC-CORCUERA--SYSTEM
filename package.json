{
  "name": "isla-sync-corcuera",
  "version": "1.0.0",
  "description": "Isla Sync Corcuera Mobile App",
  "main": "index.js",
  "scripts": {
    "sync": "npx cap sync",
    "build:android": "cd android && .\\gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}

